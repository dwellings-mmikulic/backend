# Database — for local non-Docker runs. In docker compose this is set automatically.
DATABASE_URL=postgres://dwellings:dwellings@localhost:5432/dwellings?sslmode=disable

# What this instance runs: all (default: API + worker loops), api, or worker.
# Several instances may share one database; see README "Running several
# instances" and deploy/README.md "Fleet".
# ROLE=all
# Name in logs and claims (default: hostname).
# INSTANCE_ID=
# PostgreSQL pool size (default 10, minimum 4).
# DB_MAX_CONNS=10
# Discovery pauses while this many listings wait to be rendered (<= 0 disables).
# QUEUE_HIGH_WATER=2000

# Budget windows — standard 5-field cron expression, evaluated in UTC
# (default: every 12 hours). It triggers nothing: a new window, with a fresh
# API budget, starts at each activation.
CRON_SCHEDULE=0 */12 * * *

# The share of API_BUDGET_PER_CYCLE reserved for the one-time details-API
# enrichment call per property. 0 disables enrichment entirely.
DETAILS_PER_CYCLE=50

# OpenWebNinja Real-Time Zillow Data API
ZILLOW_BASE_URL=https://api.openwebninja.com/realtime-zillow-data
ZILLOW_API_KEY=

# LocationIQ — static property maps, generated on demand by the detail
# endpoint and hosted on Bunny CDN. Leave empty to disable maps entirely.
LOCATIONIQ_API_KEY=

# Leave already-stored listings (by zpid) untouched — no re-upsert or re-render.
# Set to false to refresh existing listings (price, photos) each cycle.
SKIP_EXISTING=true

# Images — set to false to skip Bunny upload and store source Zillow URLs directly
# (handy for local/dev runs; Bunny vars below are only required when true).
IMAGES_ENABLED=true

# Bunny CDN storage
BUNNY_STORAGE_ZONE=
BUNNY_API_KEY=
BUNNY_STORAGE_HOST=storage.bunnycdn.com
BUNNY_CDN_BASE_URL=https://your-zone.b-cdn.net

# Search criteria. ZIP codes come from the built-in zip_codes rotation table
# (all 29,670 US residential ZIPs, seeded automatically on first startup) —
# there is no location env var. Each cycle searches ZIPs in rotation order:
# never-searched first (most populous first), then stalest.
SEARCH_HOME_STATUS=FOR_SALE
SEARCH_MIN_PRICE=0
SEARCH_MAX_PRICE=0
SEARCH_MIN_BEDROOMS=0
# 0 = no per-ZIP result cap (the API budget below is the real limiter).
SEARCH_MAX_RESULTS=0

# Max OpenWebNinja requests the WHOLE FLEET may spend per budget window
# (search pages + details calls, retries included). Sized so a month of
# windows fits the API plan: 150/window at two windows/day ≈ 9,000/month
# against the Pro plan's 10,000. Must be identical on every instance.
API_BUDGET_PER_CYCLE=150

# Video rendering (Roku listing videos)
VIDEO_ENABLED=true
# Frame rate of the rendered video (unset = 30). Values below 30 are rejected.
# VIDEO_FPS=30
# Threads one render may use (unset = vCPUs / LISTING_CONCURRENCY, min 1).
# VIDEO_FFMPEG_THREADS=
VIDEO_SECONDS_PER_PHOTO=4
MUSIC_DIR=assets/music
# Font for overlay text. In Docker this defaults to the bundled DejaVu font.
VIDEO_FONT_PATH=/usr/share/fonts/dejavu/DejaVuSans-Bold.ttf

# HTTP server (Roku Direct Publisher feed at /roku/feed.json, public listings
# API at /api/v1/properties*, plus /healthz)
HTTP_PORT=8080

# This server's public origin, used for the absolute URLs in the Roku feed
# (including the live channel entry). Empty = no liveFeeds entry.
PUBLIC_BASE_URL=https://api.dwellings.tv

# Linear channels — the 24/7 HLS streams assembled from the listing videos
# (/channels/*). All optional; the defaults are shown.
# LINEAR_ENABLED=true
# LINEAR_LINEUP_HOURS=6
# LINEAR_MIN_SCOPE_CLIPS=20
# LINEAR_EPG_HORIZON_HOURS=24
# Poster for the Roku live channel entry. Empty borrows the first ready
# listing's image; with neither, the live entry is omitted (Roku requires a
# thumbnail).
# LINEAR_LIVE_THUMBNAIL_URL=

# VAST ad tags from the ad server, returned as pre_roll_ad / mid_roll_ad by
# /api/v1/properties* and /channels/resolve for the Roku app to play through
# RAF (the app substitutes macros like ROKU_ADS_TRACKING_ID). Empty = null, no
# ads in that slot.
# AD_PREROLL_URL=
# AD_MIDROLL_URL=

# Viewer tracking (pseudonymous, per live-playlist poll) and /channels/resolve.
# VIEWER_SALT is required while tracking is on: a long random string, e.g.
# `openssl rand -hex 32`. Changing it re-identifies every viewer.
VIEWER_SALT=
# VIEWER_TRACKING_ENABLED=true
# VIEWER_SALT_ROTATE_DAILY=false   # true: ids unlinkable across days
# VIEWER_RETENTION_DAYS=30
# VIEWER_ADMIN_KEY protects GET /admin/viewers (raw client IPs); empty = off.
# e.g. openssl rand -hex 24
# VIEWER_ADMIN_KEY=
# MaxMind GeoLite2-City database for the IP-based channel default; empty
# turns the geo step off (resolve still returns last-watched or national).
# GEOIP_DB_PATH=/geoip/GeoLite2-City.mmdb

# Concurrency. Renders are CPU-bound (defaults to CPU count); image downloads/
# uploads are I/O-bound. Lower LISTING_CONCURRENCY on small machines.
LISTING_CONCURRENCY=
IMAGE_CONCURRENCY=8

# Outbound HTTP timeout (seconds) for Zillow + image downloads
HTTP_TIMEOUT_SECONDS=30
# Bunny upload timeout (seconds) — large for big video files
BUNNY_TIMEOUT_SECONDS=300
